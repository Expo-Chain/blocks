name: 🔏 Assinatura de documento

on:
  push:
    branches:
      - 'feature/**'

jobs:
  merge-assinado:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Checkout do repositório
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: 🔐 Importar chave GPG privada
        run: |
          mkdir -p ~/.gnupg
          chmod 700 ~/.gnupg
          echo "allow-loopback-pinentry" >> ~/.gnupg/gpg-agent.conf
          echo "pinentry-mode loopback" >> ~/.gnupg/gpg.conf
          echo "$GPG_PRIVATE_KEY" | gpg --batch --yes --pinentry-mode loopback --passphrase "$GPG_PASSPHRASE" --import
          gpgconf --kill gpg-agent
        env:
          GPG_PRIVATE_KEY: ${{ secrets.GPG_PRIVATE_KEY }}
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}

      - name: 📌 Definir chave de assinatura
        run: |
          KEY_ID=$(gpg --list-secret-keys --with-colons | grep '^sec' | cut -d':' -f5)
          echo "GPG_KEY_ID=$KEY_ID" >> $GITHUB_ENV
          git config --global user.signingkey "$KEY_ID"
        env:
          GPG_TTY: /dev/tty

      - name: ✅ Assinar e fazer merge na main
        run: |
          git config --global user.name "ExpoChain CI"
          git config --global user.email "${{ secrets.GPG_KEY_EMAIL }}"
          git config --global commit.gpgsign true
          git config --global gpg.program gpg

          export GIT_COMMITTER_NAME="ExpoChain CI"
          export GIT_COMMITTER_EMAIL="${{ secrets.GPG_KEY_EMAIL }}"
          export GPG_TTY=$(tty)
          export GIT_AUTHOR_NAME="ExpoChain CI"
          export GIT_AUTHOR_EMAIL="${{ secrets.GPG_KEY_EMAIL }}"

          git checkout main
          git pull origin main

          echo "${{ secrets.GPG_PASSPHRASE }}" | \
          gpg --batch --yes --pinentry-mode loopback --passphrase-fd 0 \
          git -c user.signingkey=${{ env.GPG_KEY_ID }} \
              -c commit.gpgsign=true \
              -c gpg.program="gpg" \
              -c gpg.pinentry-mode=loopback \
              merge --no-ff --signoff --gpg-sign=${{ env.GPG_KEY_ID }} HEAD@{1} -m "🔏 Merge assinado automático de ${GITHUB_REF##*/}"

          git push https://x-access-token:${{ secrets.GH_PAT }}@github.com/${{ github.repository }} main
        env:
          GPG_PASSPHRASE: ${{ secrets.GPG_PASSPHRASE }}
